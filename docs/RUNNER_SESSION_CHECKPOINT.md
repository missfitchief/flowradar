# Runner/Dormant Session Checkpoint (2026-07-11 ~22:00Z) — implementation handoff

Continue implementation from commit `28133f4` on `feat/runner-dormant-behavior`, worktree `C:\Users\akki\session\flowradar-runner`. This file is the complete context for a fresh session/agent.

## Frozen state — DO NOT REDO
Phase A enrichment done (347 confirmed $10M+ runners, 84 conflicting, 347 tier2 controls in `cohort_matches`, tables `token_lifecycles`/`token_enrichments`/`early_buyer_entries` populated on the PILOT DB). Task 5 tranche done: **22 real WalletBehaviorProfile rows on the pilot DB** (cohort = locally observed buyers of runner/control tokens; see `data/runner-mining/task5-report.json`). Task 0 / risk cache / shadow controller: frozen, live. Tasks 6–12: NOT implemented — that is THIS session's job.

## Environment facts
- Worktrees: dev = `flowradar-runner` (branch feat/runner-dormant-behavior); `flowradar` = LIVE shadow run (NEVER touch: no migrations, no restarts, no writes); `flowradar-gmgn`, `flowradar-helius` = other branches, leave alone.
- DBs on localhost:5439 (user/pass `flowradar`/`flowradar`): `flowradar` = LIVE (read-only at most, prefer not touching), `flowradar_test` = vitest target (auto-migrated by globalSetup), `flowradar_pilot` = pilot data (real frozen copy + RM tables; run pilots HERE; apply new migrations here via `cd packages/db && DATABASE_URL=...flowradar_pilot npx prisma migrate deploy`).
- Migrations: hand-author SQL under `packages/db/prisma/migrations/<ts>_<name>/migration.sql` + edit schema.prisma, then `migrate deploy` (NEVER `migrate dev`/reset) to flowradar_test AND flowradar_pilot only. Then `npx prisma generate` in packages/db.
- Secrets: load via dotenv from `../flowradar/.env` (worktrees don't share .env). NEVER print/commit values. BIRDEYE_API_KEY exists; Birdeye plan ~1rps, use only probe-verified endpoints (`/defi/ohlcv`, `/defi/token_overview` — see packages/db/src/runnermining/enrich.ts for the exact contract + pacing pattern).
- Tests: vitest from worktree root; DB tests = LITE-postgres harness (top-level `await probePort('localhost',5439)` + `describe.skipIf`, PREFIX-scoped fixtures, base58-safe prefixes — NO 0/O/I/l chars, cleanup in beforeEach+afterAll). Pattern examples: `packages/db/test/runnermining/universe.test.ts`, `enrich.test.ts`.
- Pilot scripts: `.mts` under scripts/, MUST guard `new URL(DATABASE_URL).pathname === '/flowradar_pilot'`. Reports → `data/runner-mining/*.json` (gitignored) + counts summarized in commits.
- Commits: isolated per logical task, `--no-verify`, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Push to `origin feat/runner-dormant-behavior`.

## Existing engine APIs to BUILD ON (do not rebuild)
- `@flowradar/db` `reconstructWalletBehavior(prisma,{chain,address})` → persists `WalletBehaviorProfile` (profileJson = `@flowradar/core` BehaviorProfile: local.tokenPositions[] with buys/sells/exits/holds/fullExitSec/receivedNotBought, funding block, provenance fields; classifierJson = hold/dump result).
- `@flowradar/core` behavior: `reconstructBehaviorProfile`, `classifyHoldBehavior`, `deriveBehaviorReceipts` (receipts engine: same_block clusters, side-wallet tiers possible/probable/strong, crews, burst exits — REUSE for Task 8 relationship evidence + Task 10 patterns).
- Lineage: `money_flow_edges` (sourceAddress/destinationAddress + sourceChain/destinationChain + `valuedUsd` nullable — honest valuation; `amountUsd` legacy may be 0), `wallet_relationships`, `monitoring_subscriptions` (priority fresh_receiver_hot), service-node classification via `lineageClassify` in core.
- Runner cohort: `token_lifecycles.runnerClass` ('verified_above_10m' etc.), `cohort_matches`, `early_buyer_entries` (currently 0 in-band — cohort caveat documented).

## Hard rules (verbatim-binding)
Shadow-only outputs; every wallet stays observation_only; no signal_eligible promotion; provider claims stay provider_claimed; unknown ≠ zero/safe/dormant/loser/non-runner; missing history ≠ dormancy; no lookahead (future events invisible to pre-entry calcs); dust/service events never reset dormancy; entity-adjust address counts; neutral wording (possible/probable linked wallet, never same-person claims); KOL/copytrader/bot/MM never boost alpha metrics; no FlowScore/threshold changes; no BSC in Solana metrics (quarantined fixture 0x1234...5678); no Dune fresh; no UI; no trading/keys; bounded+resumable+idempotent+receipted everything; tests on flowradar_test; pilots on flowradar_pilot; live DB untouched.

## Tasks 6–12 specs (implement in order; pilot each over the 22-wallet cohort, expand deterministically if healthy)
- T6 meaningful activity: pure versioned classifier (`packages/core/src/dormancy/meaningfulActivity.ts`) over wallet activity rows (local trades + money-flow edges): meaningful trade/transfer vs dust(<$1 configurable)/spam/service/pool/MM/non-economic/artifact/unknown; per-decision {classification, reasonCodes, ruleVersion, confidence, receipts}; missing valuation → unknown. DB table `wallet_activity_classifications` + builder + pilot counts.
- T7 address dormancy: pure engine (`packages/core/src/dormancy/dormancy.ts`): strictly-pre-event windows 7/14/30/90d over MEANINGFUL events only; classes fresh/covered-dormant/active/apparently-dormant-incomplete/unknown; coverage-start honesty (history start after window ⇒ incomplete, NOT dormant). Table `address_dormancy_observations` + pilot.
- T8 entity dormancy: combine T7 + relationships/receipts engine: independent_dormant_entity vs address_dormant_entity_active vs probable_side_wallet_reactivation vs fresh_funded_by_active_entity vs active vs insufficient_evidence; service-node exclusions; per-link receipts. Table `entity_dormancy_observations` + pilot.
- T9 funding/reactivation: bounded pre-event funding path from money_flow_edges (depth ≤3, nodes ≤50, no service-node expansion): direct/first funder, ts, asset/amount, valuedUsd (null honest), hops, funding→activity delay, side-wallet relationship, repeat pattern. Table `funding_reactivation_paths` + pilot.
- T10 post-entry behavior: from observed entry forward using existing tokenPositions + receipts engine: durable/partial-exit/full-exit/fast-flip/fast-dump/burst/staged-distribution/transfer-to-linked/transfer-to-service/still-holding/unknown; never transfer=sale without evidence. Table `post_entry_behaviors` + pilot.
- T11 repeat-runner mining: rank entities (entity-adjusted, duplicate-suppressed) over runner exposure vs losers/controls + one-winner dependence + behavior quality + negative evidence; small-N honesty (insufficient-evidence rows fine). Table `repeat_runner_candidates` (observation-only) + pilot.
- T12 repeat dormant-runner: join T7/T8 events with T11; repeated-independent vs side-wallet-activation vs fresh-funding vs one-off vs insufficient; never a pattern from one event. Table `dormant_runner_candidates` + pilot.

## Review discipline (credit budget)
Batch A = T6+T7+T8, Batch B = T9+T10+T11/12. Per batch: focused tests once → one full `npx vitest run` → one Codex read-only adversarial review (MCP `mcp__codex__codex`, model gpt-5.6-sol, config {"model_reasoning_effort":"xhigh"}, sandbox read-only, cwd = this worktree; frame as "defensive code-quality review"; demand file:line + failure scenarios) → fix Critical/Important → ONE re-review only if diff materially changed → isolated commits → push.
